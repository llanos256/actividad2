<%= form_with(model: comparendo) do |form| %>
  <% if comparendo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comparendo.errors.count, "error") %> prohibited this comparendo from being saved:</h2>

      <ul>
        <% comparendo.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.check_box :estado %>
  </div>

  <div class="field">
    <%= form.label :Prestamo_id %>
    <%= form.text_field :Prestamo_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
